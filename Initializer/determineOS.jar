public class Util {     
        public enum OS {
            WINDOWS, LINUX, MAC, SOLARIS
        };// Operating systems.

//establishes variable for OS
    private static OS os = null;

    //if variable for OS is empty, checks system and assigns OS type to variable.
    public static OS getOS() {
        if (os == null) {
            String operSys = System.getProperty("os.name").toLowerCase();
            if (operSys.contains("win")) {
                os = OS.WINDOWS;
            } else if (operSys.contains("nix") || operSys.contains("nux")
                    || operSys.contains("aix")) {
                os = OS.LINUX;
            } else if (operSys.contains("mac")) {
                os = OS.MAC;
            } else if (operSys.contains("sunos")) {
                os = OS.SOLARIS;
            }
        }
        return os;
    }
}

switch (Util.getOS()) {

//What pogram does if OS is windows
        case WINDOWS:
                //do windows stuff
                break;

//What program does if OS is linux
        case LINUX:
                //do linux stuff
                break;

//What program does if OS is mac
        case MAC:
                //do mac stuff
                break:

//What program does if OS is solaris
        case SOLARIS:
                //do solaris stuff
                break;
}
